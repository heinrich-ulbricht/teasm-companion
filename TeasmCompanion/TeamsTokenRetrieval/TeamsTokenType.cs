#nullable enable

namespace TeasmCompanion.TeamsTokenRetrieval
{
    public enum TeamsTokenType
    {
        Unknown,
        MyChatsAuthHeader,
        MyTenantsAuthHeader,
        MyTeamsAuthHeader
    }
}
